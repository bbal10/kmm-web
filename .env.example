    # Django Settings - Environment Variables
# Copy file ini ke .env dan isi dengan nilai yang sesuai

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Set ke 'production' untuk production, atau 'local' untuk development
# Default: local
DJANGO_ENV=local

# ============================================================================
# SECURITY
# ============================================================================
# SECRET_KEY untuk Django (WAJIB di production, minimal 50 karakter)
# Generate dengan: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# Development default sudah ada di local.py
SECRET_KEY=

# ============================================================================
# DATABASE (Production)
# ============================================================================
# Format: postgresql://username:password@hostname:port/database_name
# Contoh: postgresql://myuser:mypass@localhost:5432/mydb
# Development pakai SQLite, tidak perlu set ini
DATABASE_URL=

# ============================================================================
# ALLOWED HOSTS (Production)
# ============================================================================
# Comma-separated list of allowed hosts
# Contoh: yourdomain.com,www.yourdomain.com
# Development default: * (semua host allowed)
ALLOWED_HOSTS=

# ============================================================================
# EMAIL CONFIGURATION (Optional)
# ============================================================================
# Email backend settings untuk production
# Development pakai console backend (print ke terminal)

# SMTP server
EMAIL_HOST=smtp.gmail.com

# SMTP port (biasanya 587 untuk TLS, 465 untuk SSL)
EMAIL_PORT=587

# Use TLS? (True/False)
EMAIL_USE_TLS=True

# SMTP credentials
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Default FROM email address
DEFAULT_FROM_EMAIL=noreply@kmm-mesir.org

# ============================================================================
# REDIS CACHE (Optional - Production)
# ============================================================================
# Redis URL untuk caching di production
# Format: redis://hostname:port/db_number
# Contoh: redis://localhost:6379/0
# Jika tidak diset, akan pakai dummy cache
REDIS_URL=

# ============================================================================
# VITE CONFIGURATION (Optional)
# ============================================================================
# Set ke True untuk development dengan Vite dev server
# Set ke False untuk pakai built assets
VITE_DEV_MODE=False

# ============================================================================
# NOTES
# ============================================================================
# 1. File .env tidak boleh di-commit ke git
# 2. Pastikan .env ada di .gitignore
# 3. Untuk production, set environment variables di hosting platform
# 4. Untuk development, copy file ini ke .env dan isi yang perlu

